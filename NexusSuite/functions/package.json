{
  "name": "nexus-suite-functions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild ./index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --external:firebase-functions",
    "predeploy": "npm run build"
  },
  "dependencies": {
    "firebase-functions": "^4.9.0",
    "firebase-admin": "^12.0.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "5.6.3"
  },
  "engines": {
    "node": "20"
  }
}